name: Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  # Main repository tests
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11', '3.12']

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg
    
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -e .
    
    - name: Run tests with pytest
      run: |
        python -m pytest -v --cov=. --cov-report=term-missing
    
    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v3
      if: matrix.python-version == '3.12'
      env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  # Audio Extract module specific tests
  test-audio-extract:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11', '3.12']
    
    defaults:
      run:
        working-directory: ./audio_extract

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg
    
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-dev.txt
    
    - name: Run unit tests
      run: |
        python -m pytest tests/ -v --cov=audio_extract --cov-report=term-missing
    
    - name: Run integration tests
      run: |
        python -m pytest tests/test_*integration*.py -v
    
    - name: Run storage tests
      run: |
        python -m pytest tests/test_storage*.py -v
    
    - name: Test CLI functionality
      run: |
        # Test that CLI tools can be imported
        python -c "from audio_extract.cli import monitor"
        python -c "from audio_extract import AudioExtractor, ProcessingTracker"
    
    - name: Upload coverage reports
      uses: codecov/codecov-action@v3
      if: matrix.python-version == '3.12'
      with:
        flags: audio-extract
        name: audio-extract-coverage
      env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}